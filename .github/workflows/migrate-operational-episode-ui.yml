name: Migrar interfaz operacional de episodios

on:
  push:
    branches: [feature/geovisor-fase-8a]
    paths:
      - scripts/migrate_operational_episode_ui.py
      - .github/workflows/migrate-operational-episode-ui.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/geovisor-fase-8a
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Aplicar migración verificable
        run: python scripts/migrate_operational_episode_ui.py
      - name: Verificar arquitectura operacional
        run: |
          ! grep -q 'Escenario de sensores' app/page.tsx
          ! grep -q 'setScenario' app/page.tsx
          grep -q 'Episodios de detecciones térmicas' app/page.tsx
          grep -q 'Ficha de título minero ANM' components/operational-geovisor-map.tsx
          grep -q 'Titular / solicitante' components/operational-geovisor-map.tsx
          grep -q 'Acto administrativo' components/operational-geovisor-map.tsx
          grep -q 'ID contractual' components/operational-geovisor-map.tsx
          grep -q 'Autoridad ambiental' components/operational-geovisor-map.tsx
      - name: Confirmar y publicar la migración
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/page.tsx components/operational-geovisor-map.tsx
          if git diff --cached --quiet; then
            echo "No hay cambios por confirmar"
          else
            git commit -m "feat: enriquecer fichas de consulta del geovisor"
            git push origin HEAD:feature/geovisor-fase-8a
          fi
